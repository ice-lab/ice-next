import * as server from '@ice/runtime';
import appConfig from '@/app';
import runtimeModules from './runtimeModules';
import Document from '@/document';
import assetsManifest from './assets-manifest.json';
import routes from './routes';

export async function renderToHTML(requestContext) {
  return await server.renderToHTML(requestContext, {
    appConfig,
    assetsManifest,
    routes,
    runtimeModules,
    Document,
  });
}

export async function renderToResponse(requestContext) {
  server.renderToResponse(requestContext, {
    appConfig,
    assetsManifest,
    routes,
    runtimeModules,
    Document,
  });
}