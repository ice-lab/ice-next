import {
  Link,
  Outlet,
  useParams,
  useSearchParams,
  useLocation,
} from '@ice/runtime/router';
<% if (globalStyle) {-%>
import '<%= globalStyle %>'
<% } -%>

export {
  Link,
  Outlet,
  useParams,
  useSearchParams,
  useLocation,
};

export {
  defineAppConfig,
  useAppData,
  useData,
  useConfig,
  Meta,
  Title,
  Links,
  Scripts,
  Main,
} from '@ice/runtime';

<%- framework.imports %>

<% if (framework.exports) { -%>
export {
  <%- framework.exports %>
};
<% } -%>

export * from './types';
