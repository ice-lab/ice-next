<% if (globalStyle) {-%>
import '<%= globalStyle %>'
<% } -%>
import type { RouteConfig } from './types';
<%- framework.imports %>

interface GetConfig = () => RouteConfig;
function defineGetConfig(getConfig: GetConfig): GetConfig {
  return getConfig;
}

export {
  defineGetConfig,
  <% if (framework.exports) { -%><%- framework.exports %><% } -%>
};

export * from './types';
