<% if (globalStyle) {-%>
import '<%= globalStyle %>'
<% } -%>

export { 
  defineAppConfig, 
  useAppData, 
  useData, 
  useConfig, 
  Link, 
  useSearchParams, 
} from '@ice/runtime/miniapp';

<%- framework.imports %>

export {
<% if (framework.exports) { -%>
  <%- framework.exports %>
<% } -%>
};

export * from './types';
